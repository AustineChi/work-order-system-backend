"use strict";

const express = require('express');

const router = express.Router()


var workOrdersController = require('../controller/workOrders');

//router.get('/', workOrdersController.index);
// router.post('/save', workOrdersController.save);
// router.post('/submit', workOrdersController.submit);
// router.get('/view/:id', workOrdersController.view);
// router.post('/update/:id', workOrdersController.update);
 

module.exports = router;